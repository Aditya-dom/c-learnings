//for ( initialization; test condition; increment ) {
   //for ( initialization; test condition; increment ) {
    // set of statements
   //}   
   // set of statements
//}



#include <stdio.h>
int main()
{
    int i,j;
    //EXP 1
    //its one optional to use exp 1 because we have already intialize the variable
    //we will use exp 1 when we want intialize new variable 
    for ( ; i < 5; i++)
    {
        printf("%d %d\n",i,j);
    }
    
    return 0;
}

